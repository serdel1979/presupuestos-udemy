@model Cuenta

@{
    ViewData["Title"] = "Borrado de cuenta";
}

<div class="card">

    <div class="card-body">

        <h3 class="mb-3">¿Está seguro de eliminar a la cuenta @Model.Nombre?</h3>
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <form asp-action="EliminarCuenta">
            <input type="hidden" asp-for="Id" />
            <button class="btn btn-danger">Borrar</button>
            <a class="btn btn-secundary" asp-action="Index">Cancelar</a>
        </form>
    </div>
</div>
